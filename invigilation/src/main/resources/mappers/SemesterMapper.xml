<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.invigilation.invigilation.mapper.SemesterMapper">

    <resultMap id="BaseResultMap" type="com.invigilation.invigilation.entity.Semester">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="start_date" property="startDate"/>
        <result column="end_date" property="endDate"/>
        <result column="is_current" property="isCurrent"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM semester ORDER BY name DESC
    </select>

    <insert id="insert" parameterType="com.invigilation.invigilation.entity.Semester">
        INSERT INTO semester (name, start_date, end_date, is_current, create_time, update_time)
        VALUES (#{name}, #{startDate}, #{endDate}, #{isCurrent}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateById" parameterType="com.invigilation.invigilation.entity.Semester">
        UPDATE semester
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="startDate != null">start_date = #{startDate},</if>
            <if test="endDate != null">end_date = #{endDate},</if>
            <if test="isCurrent != null">is_current = #{isCurrent},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM semester WHERE id = #{id}
    </delete>

</mapper>
